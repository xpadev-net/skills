interface:
  display_name: "Task PR Worker"
  short_description: "Prepare delegated task PRs for merge"
  default_prompt: "Use $task-pr-worker to complete one delegated task through implementation, review, PR creation, review-hook iteration, and merge-ready handoff."
