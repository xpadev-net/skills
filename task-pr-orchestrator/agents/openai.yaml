interface:
  display_name: "Task PR Orchestrator"
  short_description: "Coordinate task threads through PR merge"
  default_prompt: "Use $task-pr-orchestrator to coordinate task threads through PR, review hook, and merge."
